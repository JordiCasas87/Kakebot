name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 7-21 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render health endpoint
        run: curl -fsS https://kakebot.onrender.com/api/health > /dev/null
